name: omni-classify
version: "3.0"
description: Classifies grocery items using a single omniscient agent (Kroger-scale 12-department taxonomy)

input:
  item_name:
    type: string
    required: true

steps:
  - id: classify
    agent: omni-classifier
    input:
      item_name: $.input.item_name

output:
  classification: $.steps.classify.output
