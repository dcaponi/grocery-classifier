name: Frozen Router
description: Routes a frozen item to frozen_meals, frozen_treats, or frozen_produce
strategy: llm
model: gpt-4.1-mini
temperature: 0

input:
  item_name:
    type: string
    required: true
