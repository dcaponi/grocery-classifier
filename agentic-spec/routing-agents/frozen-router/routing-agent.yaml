name: Frozen Router
description: Routes a frozen item to frozen_meals, frozen_treats, or frozen_produce
strategy: llm
model: qwen2.5:7b
base_url: http://localhost:11434/v1
temperature: 0

input:
  item_name:
    type: string
    required: true
