name: Beverages Classifier
description: Classifies a beverage item by sub-category
type: llm
model: gpt-4.1-mini
temperature: 0
schema: beverages_classification
user_message: "Classify this beverage item.

Item: {{item_name}}"

input:
  item_name:
    type: string
    required: true
