name: Frozen Classifier
description: Classifies a frozen item by sub-category
type: llm
model: gpt-4.1-mini
temperature: 0
schema: frozen_classification
user_message: "Classify this frozen item.

Item: {{item_name}}"

input:
  item_name:
    type: string
    required: true
